import React from 'react';
import { McpIntegrations } from 'src/sections/accountdetails/mcp/mcp-integrations';

/**
 * MCP Settings Page Wrapper
 *
 * This page displays the MCP (Model Context Protocol) integrations
 * allowing users to connect Gmail, Slack, and other services
 * for AI agents to execute actions on their behalf.
 */
const McpSettings = () => <McpIntegrations />;

export default McpSettings;